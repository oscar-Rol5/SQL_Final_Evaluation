SELECT COUNT(*) FROM TEST;

SELECT A.NAME, A.FIRSTNAME, B.SCORE FROM STUDENT A, NOTATION B WHERE A.NUMSTUD = B.NUMSTUD;

SELECT A.NAME, A.FIRSTNAME, B.SCORE, C.WORDING FROM STUDENT A, NOTATION B, MATTER C WHERE A.NUMSTUD = B.NUMSTUD;

SELECT * FROM NOTATION WHERE SCORE >= 10;

SELECT A.NAME, A.FIRSTNAME, B.SCORE FROM STUDENT A, NOTATION B WHERE A.NUMSTUD = B.NUMSTUD AND B.SCORE >=20;

SELECT A.NAME, A.FIRSTNAME, AVG(B.SCORE) AS AVERAGE_SCORE FROM STUDENT A, NOTATION B WHERE A.NUMSTUD = B.NUMSTUD GROUP BY NAME;

SELECT * FROM TEST WHERE TESTDATE BETWEEN "2014/01/01" AND "2014/06/30";

SELECT A.NUMTEST, A.TESTDATE, A.PLACE, B.WORDING FROM TEST A, MATTER B WHERE A.CODEMAT = B.CODEMAT;

SELECT A.NAME, A.FIRSTNAME, AVG(B.SCORE) AS AVERAGE_SCORE FROM STUDENT A, NOTATION B WHERE A.NUMSTUD = B.NUMSTUD GROUP BY NAME ORDER BY AVERAGE_SCORE DESC;

SELECT A.WORDING, AVG(B.SCORE) AS AVERAGE_SCORE FROM MATTER A, NOTATION B, TEST C WHERE C.CODEMAT = A.CODEMAT GROUP BY A.WORDING;

SELECT NUMTEST, AVG(SCORE) AS AVERAGE_SCORE FROM NOTATION WHERE NUMSTUD < 6 GROUP BY NUMTEST;
